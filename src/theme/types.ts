export type ThemePreference = 'light' | 'dark' | 'system';

export type ColorScheme = 'light' | 'dark';

export type AppTheme = {
  bg: string;
  surface: string;
  surfaceLight: string;
  border: string;
  text: string;
  textMuted: string;
  accent: string;
  accentDark: string;
  danger: string;
  warning: string;
  success: string;
  onAccent: string;
};
